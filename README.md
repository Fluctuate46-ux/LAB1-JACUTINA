# LAB1-JACUTINA

change git username
git config --global user.name "Jacutina"

change user identity email
git config --global user.email "jacutinajerick46@gmail.com"

Initialize repository local
git init

moving to branch called "main"
git branch -M   main

git add index.html
git commit -m "Add baseline HTML only"

git branch no-style

git branch

git checkout main

git commit
